# Endpoints

To keep program.cs clean and avoid to avoid having us work too much in there we
seperate our routes like thiss. What we want to put in here are route
definitions, input validation and responses. Endpoints should be short so don't
add anything like logic, this should be handled by the corrosponding services.