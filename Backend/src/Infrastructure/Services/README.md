# Services

In 'Services' we put the logic for our stuff. Services are dedicated classes
that handle specific tasks. They should be registered in `program.cs' so that
ASP.NET can share them across the server using dependency injection.